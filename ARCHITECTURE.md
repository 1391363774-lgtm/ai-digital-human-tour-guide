# 系统架构

## 总体架构

```text
用户浏览器
  ├─ 游客端 Vue：对话、数字人、地图、路线、收藏、历史
  └─ 管理后台 Vue：知识库、景点、数字人配置、反馈分析、数据大屏

FastAPI 后端
  ├─ api：路由、参数校验、统一响应
  ├─ services：业务逻辑与 AI 工作流
  ├─ repositories：数据库访问
  ├─ models：SQLAlchemy 实体
  ├─ schemas：Pydantic 模式
  └─ core：配置、日志、数据库、安全

AI 模块
  ├─ llm：DeepSeek / Qwen 双通道与本地兜底
  ├─ rag：文档切分、本地向量、Chroma 与词法增强检索
  ├─ prompts：知识边界、拒答与景区导游 Prompt
  └─ speech：faster-whisper、edge-tts 与分段流式合成

数据层
  ├─ PostgreSQL：业务数据
  ├─ Chroma：向量数据
  └─ 本地文件：上传资料、音频、数字人素材
```

## 核心链路

### 文本问答链路

用户问题 → 参数校验 → RAG 检索 → 知识边界判断 → 上下文构建 → LLM/本地回答 → 保存会话 → SSE 或普通响应。

### 语音问答链路

麦克风录音 → faster-whisper（失败时浏览器 ASR）→ 文本问答链路 → 首个完整句触发 TTS → 后续分段并行合成 → 顺序播放 → Web Audio 驱动 Live2D 口型与字幕。

### 知识入库链路

管理员上传 → 文件校验 → 文本抽取 → 清洗 → 分块 → Embedding → 向量库 → 入库报告。

## 降级策略

| 故障 | 降级方式 |
|---|---|
| LLM 主模型在首 token 前不可用 | 切换另一云模型；仍失败则使用本地知识库回答 |
| LLM 已经输出部分内容后失败 | 停止当前流并返回错误，不拼接另一模型的半段回答 |
| ASR 不可用 | 浏览器语音识别或文本输入 |
| TTS 不可用 | 前端显示文本并使用浏览器 TTS |
| 地图 SDK 不可用 | 显示静态路线卡片 |
| Live2D 模型/CDN 不可用 | 回退 SVG 数字人，不阻塞问答 |
