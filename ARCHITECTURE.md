# 系统架构

## 总体架构

```text
用户浏览器
  ├─ 游客端 Vue：对话、数字人、地图、路线、收藏、历史
  └─ 管理后台 Vue：知识库、景点、数字人配置、反馈分析、数据大屏

FastAPI 后端
  ├─ api：路由、参数校验、统一响应
  ├─ services：业务逻辑与 AI 工作流
  ├─ repositories：数据库访问
  ├─ models：SQLAlchemy 实体
  ├─ schemas：Pydantic 模式
  └─ core：配置、日志、数据库、安全

AI 模块
  ├─ llm：大模型客户端
  ├─ rag：文档切分、Embedding、检索
  ├─ prompts：Prompt 模板
  ├─ tools：工具调用
  └─ memory：对话记忆

数据层
  ├─ PostgreSQL：业务数据
  ├─ Chroma：向量数据
  └─ 本地文件：上传资料、音频、数字人素材
```

## 核心链路

### 文本问答链路

用户问题 → 参数校验 → 意图识别 → RAG 检索 → 上下文构建 → LLM 生成 → 保存会话 → 前端展示。

### 语音问答链路

麦克风录音 → ASR → 文本问答链路 → TTS → 音频播放 → 数字人口型与字幕同步。

### 知识入库链路

管理员上传 → 文件校验 → 文本抽取 → 清洗 → 分块 → Embedding → 向量库 → 入库报告。

## 降级策略

| 故障 | 降级方式 |
|---|---|
| LLM 主模型不可用 | 切换 Qwen 备用模型 |
| ASR 不可用 | 允许文本输入 |
| TTS 不可用 | 前端显示文本并使用浏览器 TTS |
| 地图 SDK 不可用 | 显示静态路线卡片 |
| 数字人增强服务不可用 | 回退 2D 数字人 MVP |
