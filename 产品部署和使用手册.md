# 灵山胜境景区导览服务AI数字人 -- 产品部署和使用手册

---

## 一、环境要求

| 依赖项            | 最低版本  | 说明                                      |
| ----------------- | --------- | ----------------------------------------- |
| Python            | 3.10+     | 后端运行环境                              |
| MySQL             | 8.0+      | 关系型数据库，存储用户、问答记录等        |
| Redis             | 7.0+      | 缓存与会话管理                            |
| Milvus            | 2.4+      | 向量数据库，支撑 RAG 知识库语义检索       |
| Docker            | 20.10+    | 容器化部署（可选，推荐）                   |
| Docker Compose    | 2.0+      | 编排多容器服务（可选，推荐）              |
| 现代浏览器        | —         | Chrome 90+、Edge 90+，需支持 WebGL/Web Speech API |

---

## 二、快速部署（Docker 方式，推荐）

### 2.1 克隆项目

```bash
git clone <项目仓库地址>
cd 灵山胜境AI数字人
```

### 2.2 配置环境变量

```bash
cp .env.example .env
```

编辑 `.env` 文件，填入以下关键配置：

```env
# 数据库连接
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=lingshan_guide

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Milvus
MILVUS_HOST=127.0.0.1
MILVUS_PORT=19530

# 大语言模型 API（通义千问）
DASHSCOPE_API_KEY=sk-your-api-key
LLM_MODEL=qwen-vl-max

# TTS 语音合成（可选）
TTS_API_KEY=your-tts-key
TTS_API_URL=https://your-tts-endpoint

# 服务端口
BACKEND_PORT=8000
FRONTEND_PORT=8080
```

### 2.3 一键启动

```bash
docker-compose up -d
```

该命令会自动拉取镜像并启动以下服务：
- **backend** — FastAPI 后端服务（端口 8000）
- **mysql** — MySQL 数据库（端口 3306）
- **redis** — Redis 缓存（端口 6379）
- **milvus** — Milvus 向量数据库（端口 19530）
- **nginx** — 前端静态资源服务 + 反向代理（端口 8080）

### 2.4 验证部署

浏览器访问：

```
http://localhost:8080
```

若能看到灵山胜境首页，说明部署成功。

### 2.5 初始化知识库

首次部署需要构建向量知识库：

```bash
docker-compose exec backend python -m scripts.build_knowledge_base
```

---

## 三、手动部署

### 3.1 后端部署

#### 3.1.1 安装 Python 依赖

```bash
cd backend
pip install -r requirements.txt
```

#### 3.1.2 配置环境变量

```bash
cp .env.example .env
# 编辑 .env，填入数据库连接信息、API 密钥等（参考 2.2 节）
```

#### 3.1.3 初始化数据库

确保 MySQL、Redis、Milvus 已启动并按 `.env` 配置连接。

```bash
# 建表（如使用 ORM 自动迁移）
python -m app.main
# 或手动执行 SQL 建表脚本
```

#### 3.1.4 构建知识库

```bash
python -m scripts.build_knowledge_base
```

#### 3.1.5 启动后端服务

```bash
python -m app.main
```

后端服务运行在 `http://localhost:8000`，API 文档地址为 `http://localhost:8000/docs`。

### 3.2 前端部署

本项目前端为**纯静态 HTML 页面**，无需 Node.js 构建工具。提供两种部署方式：

#### 方式一：使用 HTTP 服务器（推荐）

```bash
# 使用 Python 内置 HTTP 服务器
cd design/lingshan-guide
python -m http.server 8080

# 或使用 nginx
# nginx 配置示例：
# server {
#     listen 8080;
#     root /path/to/design/lingshan-guide;
#     index pages/home.html;
#     location /api/ {
#         proxy_pass http://localhost:8000/;
#     }
# }
```

访问 `http://localhost:8080/pages/home.html`。

#### 方式二：直接在浏览器打开

```
双击打开 design/lingshan-guide/pages/home.html
```

> **注意**：直接打开 HTML 文件时，语音功能（Web Speech API）在某些浏览器中可能受限，建议使用 HTTP 服务器方式部署以获得完整功能。

---

## 四、功能使用说明

### 4.1 游客端（移动端适配）

#### 首页

- 展示灵山胜境景区介绍、今日推荐内容
- 点击 **"开始导览"** 按钮进入 AI 数字人对话页面
- 底部导航栏可切换至"游览路线"、"拈花湾"、"我的"等页面

#### AI 导览页（核心功能）

- **灵小仙数字人**：页面中展示 3D 数字人形象
- **对话交互**：
  - 文字输入：在底部输入框输入问题，点击发送
  - 语音输入：点击麦克风按钮，说话后自动识别为文字并发送
- **3D 数字人操作**：
  - 鼠标/手指拖动：移动数字人位置
  - 滚轮/双指缩放：旋转数字人
- 支持的提问类型示例：
  - "灵山大佛有多高？"
  - "推荐一条适合半天的游览路线"
  - "梵宫里面有什么看点？"

#### 游览路线页

- 查看景区全景地图
- 切换 3 条推荐路线：
  - 经典朝圣路线
  - 文化深度游路线
  - 亲子休闲路线
- 点击地图上的景点标记可查看景点简介

#### 景点详情页

- 展示各景点的详细介绍、高清图片
- 包含历史文化背景、参观建议等

#### 拈花湾页

- 灵山胜境姐妹景区 -- 拈花湾小镇介绍
- 展示拈花湾的特色文化与游览推荐

#### 我的（个人中心）

- 门票信息查看
- 游览历史记录
- 个人设置（需登录后使用）

---

### 4.2 管理后台（桌面端适配）

访问地址：`http://localhost:8080/pages/admin.html`

#### 数据大屏概览

- 实时展示服务人次统计
- 游客满意度趋势图表
- 热门问答 Top 排行
- 访问量与活跃用户数据

#### 知识库管理

- 查看和维护 22 个景点的专业知识条目
- 支持新增、编辑、删除知识条目
- 知识库内容变更后可触发重新向量化

#### 数字人形象管理

- 配置灵小仙的头像图片
- 上传/更换 3D 模型文件（.glb 格式）
- 配置语音参数（音色、语速）
- 自定义欢迎语和默认回复

#### 游客感受度报告

- 游客关注点分析（哪些景点/话题被询问最多）
- 情感趋势分析（正面/中性/负面反馈比例）
- 服务改进建议（基于数据分析自动生成）

---

## 五、常见问题

| 问题                     | 可能原因                          | 解决方案                                                                 |
| ------------------------ | --------------------------------- | ------------------------------------------------------------------------ |
| 3D 模型加载失败          | 模型文件缺失或路径不正确          | 检查 `design/lingshan-guide/assets/xiuxian-3d.glb` 文件是否存在           |
| 语音功能不可用           | 非 HTTPS 环境或浏览器未授权麦克风 | 部署到 HTTPS 环境；使用 Chrome 浏览器并在地址栏左侧允许麦克风权限          |
| 知识库问答不准确         | 知识库内容过期或不足              | 在管理后台"知识库管理"页面更新相关内容，重新构建向量索引                    |
| 后端服务启动失败         | 依赖未安装或数据库连接失败        | 检查 `requirements.txt` 是否安装完毕；确认 MySQL/Redis/Milvus 服务已启动   |
| 前端页面空白             | 静态资源路径错误                  | 确认通过 HTTP 服务器访问而非直接打开文件；检查浏览器控制台错误信息          |
| Docker 容器无法启动      | 端口冲突或内存不足                | 检查 8080/8000/3306/6379/19530 端口是否被占用；确保 Docker 可用内存 >= 4GB |
| AI 回复速度慢            | LLM API 响应延迟或网络问题        | 检查网络连接；确认 DASHSCOPE_API_KEY 有效且余额充足                        |

---

## 六、项目目录结构说明

```
灵山胜境AI数字人/
│
├── README.md                          # 项目说明文件
├── docker-compose.yml                 # Docker 编排配置
├── 系统架构设计文档.md                  # 系统架构详细设计
├── 数据分析报告.txt                    # 数据分析报告
├── 产品部署和使用手册.md               # 本文档
│
├── backend/                           # 后端服务
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                    # FastAPI 应用入口
│   │   ├── api/                       # API 路由
│   │   │   ├── __init__.py
│   │   │   ├── chat.py                # AI 对话接口
│   │   │   ├── knowledge.py           # 知识库管理接口
│   │   │   ├── avatar.py              # 数字人配置接口
│   │   │   └── analytics.py           # 数据分析接口
│   │   ├── core/                      # 核心配置
│   │   │   ├── __init__.py
│   │   │   ├── config.py              # 配置管理（读取 .env）
│   │   │   └── database.py            # 数据库连接
│   │   ├── models/                    # 数据模型
│   │   │   ├── __init__.py
│   │   │   └── models.py              # SQLAlchemy / Pydantic 模型定义
│   │   └── services/                  # 业务逻辑
│   │       ├── __init__.py
│   │       └── chat_service.py        # 对话服务（LLM + RAG）
│   ├── scripts/                       # 脚本工具
│   │   ├── __init__.py
│   │   ├── build_knowledge_base.py    # 知识库构建脚本（文本切分+向量化）
│   │   └── knowledge_chunks.json      # 知识库切分后的文本块
│   ├── .env                           # 环境变量配置（不提交到 Git）
│   ├── .env.example                   # 环境变量示例
│   ├── Dockerfile                     # 后端 Docker 镜像构建
│   └── requirements.txt               # Python 依赖清单
│
├── design/                            # 前端设计资源
│   ├── lingshan-guide/                # 游客端前端
│   │   ├── assets/                    # 静态资源
│   │   │   ├── xiuxian-3d.glb         # 灵小仙 3D 模型文件
│   │   │   ├── xiuxian-3d-mouth.glb   # 灵小仙口型动画模型
│   │   │   ├── xiuxian-avatar.jpg     # 灵小仙头像
│   │   │   ├── xiuxian-3d-model.jpg   # 3D 模型预览图
│   │   │   ├── lingshan-buddha.jpg    # 灵山大佛图片
│   │   │   ├── lingshan-map.jpg       # 景区地图
│   │   │   ├── baizi-milefo.jpg       # 百子弥勒图片
│   │   │   ├── fangong-palace.jpg     # 梵宫图片
│   │   │   ├── fojiao-museum.jpg      # 佛教博物馆图片
│   │   │   ├── jiulong-guanyu.jpg     # 九龙灌浴图片
│   │   │   ├── xiangfu-temple.jpg     # 祥福禅寺图片
│   │   │   └── knowledge.json         # 前端知识库数据
│   │   ├── pages/                     # HTML 页面
│   │   │   ├── home.html              # 首页
│   │   │   ├── guide.html             # AI 导览对话页
│   │   │   ├── route.html             # 游览路线页
│   │   │   ├── exhibit-detail.html    # 景点详情页
│   │   │   ├── nianhua.html           # 拈花湾介绍页
│   │   │   ├── profile.html           # 个人中心页
│   │   │   └── admin.html             # 管理后台入口页
│   │   ├── colors_and_type.css        # 全局样式（配色+字体）
│   │   └── lingshan-guide.design      # 设计配置文件
│   │
│   ├── lingshan-admin/                # 管理后台前端
│   │   ├── assets/
│   │   │   └── xiuxian-avatar.jpg     # 数字人头像
│   │   ├── pages/
│   │   │   ├── dashboard.html         # 数据大屏
│   │   │   ├── knowledge.html         # 知识库管理
│   │   │   ├── avatar.html            # 数字人形象管理
│   │   │   └── analytics.html         # 游客感受度报告
│   │   ├── colors_and_type.css        # 后台样式
│   │   └── lingshan-admin.design      # 后台设计配置
│   │
│   └── assets/
│       └── xiuxian-avatar.jpg         # 公共头像资源
│
└── 知识库/                            # 原始知识库文档
    └── 灵山胜境完整知识库.md           # 景区完整知识文本（22 个景点）
```

---

> **文档版本**：v1.0  
> **更新日期**：2026 年 7 月  
> **适用版本**：灵山胜境AI数字人导览系统 全部版本